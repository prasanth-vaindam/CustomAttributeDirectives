import { ApplyclassDirective } from './applyclass.directive';

describe('ApplyclassDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyclassDirective();
    expect(directive).toBeTruthy();
  });
});
